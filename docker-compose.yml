version: '3.8'

services:
  siop-app:
    build: .
    container_name: eaton_siop_inventory_hub
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
